Program3 creates optimized PDF file. Creates JP@ images from RAW and Tiffs. Creats mix files for every image in the transformation path RAW->JP2 and RAW->Tiff->JP2. It alse creates a METS file for each transformation path. it deletes the RAW images and assembles data into two folders, an ACCESS and a PRESERvation folder. converts the folders into BAGIT digital bags and then moves the bags to the storage server.
